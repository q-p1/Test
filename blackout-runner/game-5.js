function drawInteractable(o){ctx.save();ctx.translate(o.x,o.y);let col=o.type==='heal'?'#72f7a2':o.type==='shop'?'#ffd166':'#66e8ff';ctx.shadowBlur=24;ctx.shadowColor=col;ctx.strokeStyle=col;ctx.lineWidth=2;ctx.fillStyle='#0b1422';ctx.beginPath();ctx.roundRect(-30,-25,60,50,12);ctx.fill();ctx.stroke();ctx.shadowBlur=0;ctx.fillStyle=col;ctx.font='900 20px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(o.used?'×':o.type==='heal'?'+':o.type==='shop'?'◆':'◈',0,1);ctx.restore()}
function drawParticle(p){let a=clamp(p.l/p.m,0,1);ctx.save();ctx.globalAlpha=a;if(p.type==='slash'){ctx.strokeStyle='#ff6f98';ctx.lineWidth=12*a;ctx.beginPath();ctx.arc(p.x,p.y,(p.range||112)*.64,p.a-.7,p.a+.7);ctx.stroke()}else if(p.type==='warn'){ctx.strokeStyle='#ff4368';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x+Math.cos(p.a)*180,p.y+Math.sin(p.a)*180);ctx.stroke()}else if(p.type==='ghost'){ctx.fillStyle='#55e9ff';ctx.beginPath();ctx.arc(p.x,p.y,14*a,0,6.28);ctx.fill()}else if(p.type==='muzzle'){ctx.fillStyle='#c9fbff';ctx.beginPath();ctx.arc(p.x,p.y,10*a,0,6.28);ctx.fill()}else{ctx.fillStyle=p.c||'#fff';ctx.beginPath();ctx.arc(p.x,p.y,3+5*a,0,6.28);ctx.fill()}ctx.restore()}
function drawMini(){mctx.clearRect(0,0,180,112);mctx.fillStyle='#050912bb';mctx.fillRect(0,0,180,112);mctx.strokeStyle='#31486d';mctx.strokeRect(1,1,178,110);let sx=180/W,sy=112/H;for(let w of walls){mctx.fillStyle='#263858';mctx.fillRect(w.x*sx,w.y*sy,w.w*sx,w.h*sy)}for(let e of enemies){mctx.fillStyle=e.kind==='boss'?'#ffd166':'#ff5b7d';mctx.beginPath();mctx.arc(e.x*sx,e.y*sy,e.kind==='boss'?4:2,0,6.28);mctx.fill()}mctx.fillStyle='#5cecff';mctx.beginPath();mctx.arc(player.x*sx,player.y*sy,3,0,6.28);mctx.fill()}
function loop(now){if(gameState!=='play')return;let dt=Math.min(.033,(now-last)/1000);last=now;if(slowmo>0)dt*=.5;update(dt);draw();requestAnimationFrame(loop)}