name: Heavy QA

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - feat/routine-pwa

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps webkit
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:unit
      - run: npm run build
      - run: npm run test:e2e
      - name: Save visual QA and browser report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: routine-heavy-qa
          path: |
            visual-qa/
            playwright-report/
            test-results/
          if-no-files-found: ignore
